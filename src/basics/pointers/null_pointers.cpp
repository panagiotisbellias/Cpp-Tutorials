#include <iostream>

using namespace std;
int main () {
	int  *ptr = NULL;
	//if(ptr)     // succeeds if p is not null
	//if(!ptr)    // succeeds if p is null
	cout << "The value of ptr is " << ptr ;

	return 0;
}